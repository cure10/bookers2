<%= form_with model:@book, url:"/books/#{@book.id}", local:true do |book| %>
  <h4>Title</h4>
  <%= book.text_field :title %>
  <h4>Opinion</h4>
  <%= book.text_area :body %>
  <%= book.submit 'UpdateBook' %>
<% end %>