  <% if @book.errors.any? %>
    <%= @book.errors.count %>error
    <% @book.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
  <% end %><%= form_with model:@book, local:true do |book| %>
  <h1>Editing Book</h1>
  <h4>Title</h4>
  <%= book.text_field :title %>
  <h4>Opinion</h4>
  <%= book.text_area :body %>
  <div>
  <%= book.submit 'Update Book' %>
  </div>
  <div><%= link_to "Show",book_path(@book) %></div>
  <div><%= link_to "Back",books_path %></div>
<% end %>