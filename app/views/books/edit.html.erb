  <% if @book.errors.any? %>
    <%= @book.errors.count %>error
    <% @book.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
  <% end %><%= form_with model:@book, url:"/books/#{@book.id}", local:true do |book| %>
  <h4>Title</h4>
  <%= book.text_field :title %>
  <h4>Opinion</h4>
  <%= book.text_area :body %>
  <div>
  <%= book.submit 'UpdateBook' %>
  </div>
  <div><%= link_to "show",book_path(book) %></div>
  <div><%= link_to "Back",books_path %></div>
<% end %>